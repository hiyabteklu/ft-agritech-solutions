import ContactClient from '@/components/ContactClient';

export const metadata = {
  title: 'Contact',
  description: 'Contact FT Agri-Tech Solutions — partnerships, sales, and support.',
};

export default function ContactPage() {
  return <ContactClient />;
}
